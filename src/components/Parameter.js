import React from 'react';

const styles = {
    summary: {
        fontSize: '20px'
    }
};

function Parameter({param, theme}) {
    return (
        <div>
            {/* Parameter */}
        </div>
    );
}

export default Parameter;