import React from 'react';

const styles = {
    summary: {
        fontSize: '20px'
    }
};

function Response({rsp, theme}) {
    return (
        <div>
            {/* Response */}
        </div>
    );
}

export default Response;